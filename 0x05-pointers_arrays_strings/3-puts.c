#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _puts - prints out a string followed by new line
 * @str: is the string
 * Return: no return value
 */
void _puts(char *str)
{
	 puts(str);
}
