#include "main.h"
#include <string.h>

/**
 * _strlen - checks the lenght of a string
 * @s: is pointer for str
 * Return: the length of the string
 */

int _strlen(char *s)
{
	int p;

	p = strlen(s);
	return (p);
}
