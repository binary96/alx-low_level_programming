#include "main.h"

/**
 * mul - multiples given integers
 * @a: is the integers given
 * @b: the second integer
 * Return: c
 */

int mul(int a, int b)
{
	int c;

	if (1 > 0)
	{
		c = a * b;
	}
	return (c);
}

